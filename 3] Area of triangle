base = float(input("Enter a base of triangle:"))
height = float(input("Enter a height of traingle:"))

def area():
    area = 0.5 * base * height
    return area
result = area()
print("The area of the triangle is:",result)
